import { Firestore } from "@firebase/firestore"
import { UUID } from "crypto"

export const getTodos = () => {

}

export const addTodo = () => {

}

export const editTodo = (id: UUID) => {

}

export const removeTodo = (id: UUID) => {

}
